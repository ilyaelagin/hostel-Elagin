version: "3"

volumes:
  hostel-DB:

services:
  app:
    build: .
    ports:
      - 8080:8080
    container_name: app
    links:
      - activemq
  activemq:
    image: rmohr/activemq
    ports:
      - 61616:61616
      - 8161:8161
